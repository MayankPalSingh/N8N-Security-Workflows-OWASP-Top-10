# =============================================================================
# FAKE DEVELOPER CONFIG — FOR DEMO PURPOSES ONLY
# These are entirely fictitious credentials used to simulate MCP01 attack scenarios.
# Do NOT use real credentials in MCP server memory or tool calls.
# =============================================================================

# OpenAI API Key (FAKE)
OPENAI_API_KEY=sk-prod-FAKE1234abcd5678efgh9012ijkl3456mnop7890

# AWS Access Keys (FAKE)
AWS_ACCESS_KEY_ID=AKIAFAKE1234567890AB
AWS_SECRET_ACCESS_KEY=FaKeAwSsEcReTkEy1234567890abcdefghijklmn

# GitHub Personal Access Token (FAKE)
GITHUB_TOKEN=ghp_FAKE1234567890abcdefghijklmnopqrstuvwxyz

# Database Connection String (FAKE)
DATABASE_URL=postgresql://admin:FakeP@ssw0rd123@prod-db.internal:5432/app_production

# Stripe Secret Key (FAKE)
STRIPE_SECRET_KEY=sk_live_FAKE1234567890abcdefghijklmnopqrstuvwxyz

# Internal API Secret (FAKE)
INTERNAL_API_SECRET=internal-svc-FAKE-abc123-xyz789-secret-token

# =============================================================================
# HOW THIS FILE IS USED IN THE DEMO:
#
# Scenario A (Prompt Recall):
#   A "developer" agent stores OPENAI_API_KEY via mcp01_store_secret_vulnerable.
#   An "attacker" agent then calls mcp01_recall_context_vulnerable and retrieves it.
#
# Scenario B (Log Scraping):
#   A "developer" agent calls mcp01_call_api_vulnerable with GITHUB_TOKEN.
#   An "attacker" agent calls mcp01_get_logs_vulnerable and reads the token from logs.
#
# Scenario C (Context Poisoning):
#   An "attacker" injects an instruction via mcp01_inject_instruction_vulnerable.
#   A later innocent "user" session triggers mcp01_respond_with_example_vulnerable
#   and the model leaks STRIPE_SECRET_KEY from context.
# =============================================================================
